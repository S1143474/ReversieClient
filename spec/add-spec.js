describe("Addition", () => {
    it("is 1", () => {
        expect(1).toBe(1);
    });

    it("can add 2 to 3", () => {
        expect(2 + 3).toBe(5);
    });

    it("can 6 minus 3", () => {
        expect(6 - 3).toBe(2);
    });
});